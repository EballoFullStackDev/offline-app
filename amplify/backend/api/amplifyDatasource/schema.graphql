type Message @model {
  id: ID!
  title: String!
  color: String
  image: String
  createdAt: String
}

type Todo @model {
  id: ID!
  task: String!
  status: TodoStatus!
}

enum TodoStatus {
  COMPLETED
  INPROGRESS
}